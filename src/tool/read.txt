Read a file or directory.

Rules:
- use absolute paths
- read larger chunks when you need context
- use grep or glob first when the path or target is unknown
- parallelize reads when multiple known files are needed
