# OpenCode-Thrifty

OpenCode-Thrifty trims the extra context OpenCode adds, so you start with less noise and more room for your own work.

## How to use

- Install it with `opencode plugin opencode-thrifty --global`.
- Use OpenCode normally after install.
- Clone the repo only if you want to customize it.
